ktor {
  deployment {
    port = 8081
    port = ${?PORT}
  }
  application {
    modules = [com.shop.auth.ApplicationKt.module]
  }
}

db {
  url = "jdbc:postgresql://localhost:5432/shopdb"
  url = ${?DATABASE_URL}
  user = "postgres"
  user = ${?DB_USER}
  password = "postgres"
  password = ${?DB_PASSWORD}
}

jwt {
  secret = "your-super-secret-key-change-in-production"
  secret = ${?JWT_SECRET}
  issuer = "shop-auth"
  audience = "shop-users"
}